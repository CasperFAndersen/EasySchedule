<Window x:Class="DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopClient"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="MainWindow" Height="768" Width="1366">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <local:Calendar x:Name="Calendar" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"/>
        <ListView Name="EmployeeList" Grid.Column="1"></ListView>
        <Button x:Name="BtnSaveTemplateSchedule" Content="Button" HorizontalAlignment="Left" Margin="565,308,0,0" VerticalAlignment="Top" Width="75" Click="BtnSaveTemplateSchedule_Click"/>
        <ComboBox x:Name="CBoxDepartment" ItemsSource="{Binding departmentList}" HorizontalAlignment="Left" Margin="133,158,0,0" VerticalAlignment="Top" Width="130" Height="26" SelectionChanged="CBoxDepartment_SelectionChanged"/>
        <TextBox x:Name="TxtBoxTemplateScheduleName" HorizontalAlignment="Left" Height="22" Margin="133,110,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
    </Grid>
</Window>
