<UserControl x:Class="DesktopClient.Views.EmployeeViews.CreateEmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopClient"
             xmlns:employeeViews="clr-namespace:DesktopClient.Views.EmployeeViews"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">
    <Grid MinWidth="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="150"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <employeeViews:EmployeeView Grid.Row="0">
            </employeeViews:EmployeeView>
        <Button  x:Name="BtnSaveEmployee" Content="Save" Grid.Row="1" Grid.Column="0"  Click="BtnSaveEmployee_Click" ></Button>
    </Grid>
</UserControl>

